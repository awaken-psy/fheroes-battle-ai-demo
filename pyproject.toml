[project]
name = "battle-ai-demo"
version = "0.1.0"
description = "HoMM2 Battle AI Demo — tactical AI reimplementation"
requires-python = ">=3.11"
dependencies = [
    "pygame>=2.6",
]

[project.scripts]
battle-ai-demo = "main:main"
